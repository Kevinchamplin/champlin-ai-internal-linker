(function () {
    'use strict';
    // Reserved for future client-side report enhancements (live filter, CSV export).
    // Server-side rendering is sufficient for v1.1.0; this stub keeps the asset
    // pipeline + nonce wiring in place.
    if (typeof window.wp === 'undefined') {
        return;
    }
})();
